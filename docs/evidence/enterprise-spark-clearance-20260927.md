# 企业专属 Spark 清场记录（2026-09-27）

## 范围与操作

- 仅处理企业专属池的 `spark-57f5-98a504ed`（`192.168.2.178`）与 `spark-368c-0f2ee8b2`（`192.168.2.179`）；未动试用池 `.176/.177`、MiniMax 和 ComfyUI。
- 核对两台 GPU 的占用均来自同一套双机 `GLM-5.3-Flash-EXL3` Docker 服务；Kubernetes 上没有其他企业工作负载，只有平台节点组件。
- 先正常停止 `.179` 的 `glm53-exl3-worker`，再正常停止 `.178` 的 `glm53-exl3-head`。两者均以退出码 0 停止，容器定义、镜像和模型文件保留。
- `.178` 的专用 `glm53-nfs` 原设 `restart=unless-stopped`，已改为 `restart=no`。首次停止因 `.179` 上只读测试挂载 `/mnt/nfstest` 仍占用而失败；检查挂载无用户进程、无子挂载后，在 `.179` 惰性卸载该测试挂载，再次停止 NFS 容器，退出码 0。没有删除 NFS 导出目录或权重。

## 停止后的独立核验

- 两台 `glm53-exl3-*` 及 `glm53-nfs` 容器均为 `Exited (0)`；没有正在运行的其他 Docker 容器或 NVIDIA 计算进程。
- 两台各约 `117.8 GiB` `MemAvailable`，Kubernetes 节点均 `Ready`，节点标签均为 `lunanexa.io/usage-pool=enterprise-dedicated`。
- 公网管理端「租约 → 承诺函整机下发」刷新后，`.178/.179` 均可勾选；试用池 `.176/.177` 仍禁选。公网 ComfyUI `http://106.39.18.146:5005/` 返回 HTTP 200。
- 没有将清场等同于下发：管理端现有两张承诺函订单均显示「订单已关闭，需重新下单」，下发按钮禁用，当前「0 台机器租用中」。未创建新订单、未续用过期订单、未伪造签署／审批／到账记录、未给任何企业授予节点访问。

## 尚需确定

需明确此次两台机器的企业／租户、租期，以及使用新承诺函商业订单还是平台私有云管理员授权。资源已空闲，但未获得这些绑定信息和有效授权前，不能声称已经「出租给我们」。
