# 承诺函整机下发加固与现场核验（2026-09-27）

## 本轮修复

- 承诺函整机分配只接受节点代理心跳中标注的 `enterprise-dedicated` 资源池；`.176/.177` 试用池不能被下发，即使将来释放内存也一样。
- 企业承诺函的整机预留由有效订单、签署执行证据与租期维持，不再错误地依赖购买人个人 WebIDE 租约；其他企业成员仍须取得自己的 Developer 工作区授权才能启动应用。
- 普通个人资源预留按精确租约和授权 ID 校验，避免同一用户存在多张租约时误用第一张租约而提前回收。
- 管理端区分订单关闭、订单到期、租期不可用；节点选择显示资源池错误或内存不足的具体原因。节点总览在宿主可用内存低于 32 GiB 时不再显示“可进入预检”。

## 验证与发布

- `moon check --target native --deny-warn`、`moon check --target js --deny-warn` 通过；`moon info`、`moon fmt` 已运行。
- JS 管理端与 UI 测试 `172/172` 通过；Linux 原生 API 定向测试：资源预留授权 `2/2`，承诺函预检及权限 `4/4` 通过。默认小栈运行 API 测试会使 MoonBit 编译器栈溢出；使用 `ulimit -s unlimited` 重跑后测试通过，这是工具链编译问题，不是测试断言失败。
- 控制器、管理端、企业端镜像均已推送到私有仓库，删除目标本地标签后从仓库成功拉回。当前部署摘要：
  - 控制器 `moon/lunanexa-control@sha256:6364a2e2a0a5ba2fdfd9ec1ed9063113dfb585bedc3fb6205c1aba9acaeeafdc`
  - 管理端 `moon/lunanexa-web@sha256:0b81e287aa4999e24954aa13f0af84b2310067179265adae6694e3efd9532b57`
  - 企业端 `moon/lunanexa-web@sha256:193fa988963d25eae2ba72b9fe7cbdf164aff4af5f62e5d556649cf976cbfcc6`
- 三个 Deployment 均 `1/1 Ready`。公网 `:8443/mana/`、`:8443/user/`、`:8443/docs/` 与 ComfyUI `:5005/` 返回 HTTP 200；管理端与企业端 JS 的公网 SHA-256 与本次构建产物一致。
- 公网管理端账户会话中，资源选择器实测显示 `.176/.177` 为“非企业专属池”，`.178/.179` 为“可用内存不足”。刷新控制器数据后，两张旧承诺函订单均显示“订单已关闭，需重新下单”，下发按钮保持禁用。

## 不能写成已通过的部分

- 现场没有当前有效、已签署、经甲方审批且完成独立到账核验的承诺函订单；两张旧订单均已关闭。本轮没有代签、伪造付款或进行真实整机分配。
- `.178/.179` 仍有宿主级 GLM 双机进程，宿主可用内存约 8.8–10.8 GiB；未中断未知在用任务。必须协调腾空后才能下发并实测企业 IaaS/PaaS/MaaS 自助启停。
- `.177` 的签名心跳曾显示“资源池未分类”；下方续查已确认并修复当前显示。部署中的节点代理镜像仍旧于源码里的逐心跳清单重读修复，日后修改 ConfigMap 时需升级代理镜像或仅重启对应代理；不要因此重启 MiniMax 试用 Pod。
- 企业端公网入口已验证可达，但本轮未登录一个拥有新有效订单的企业账户，不能称双侧完整业务链已验收。
- 从本测试机访问公网标准 443 仍失败；下方续查定位到公网入口路径，不在证书或本次应用镜像。

旧摘要可用于回滚：控制器 `51041c3ff71fa14b1eb5d54cd074fa85d91d1e4bcfa0cca340cdaa67130c12b1`，管理端 `3ac4273e60f78d8e32e5e4c6a36265b6fbb6c548115935f1676057d165551959`，企业端 `d9ae878356a04f9593d7b6f548e72fa80c39f65039062770408c16acf4a2a61a`。

## 后续修复与复验（同日）

- `.177` 节点清单 ConfigMap 原本已写 `lunanexa.io/usage-pool=trial`，但旧节点代理仍上报缓存标签。仅滚动该节点的 `lunanexa-node-agent-spark-3782-feee26eb` Deployment 后，公网管理端总览实测显示“试用池”；`minimaxh3-ref2va-787ffd645d-9p6d5` Pod 名称、Ready `1/1` 和零重启保持不变，公网 ComfyUI `:5005/` 返回 200。没有重启模型服务。
- 修复承诺函年度／季度等档位被 30 天订单期限暗中截短的错误：企业端按所选档位加 31 天办理窗口创建订单；年度订单上限为 396 天，普通订单仍为 366 天；已签租期若晚于订单授权截止，服务端拒绝下发而不生成缩水租约。整机授权、主租约判定和合同运维到期展示统一按北京时间自然日边界处理。
- 原生 API／合同定向测试 `28/28`、合同存储测试 `15/15`、JS 界面测试 `138/138`；`moon check --target native --deny-warn`、`moon check --target js --deny-warn`、`moon info`、`moon fmt` 均通过。代码提交 `e8f5288` 已推送 GitHub、GitLab `main`。
- 三个候选镜像均先推送至集群私有仓库，再删除本地目标标签并从仓库拉回，才滚动 Deployment。新摘要：控制器 `moon/lunanexa-control@sha256:ef4d77171879e76ff44d15548bf1b7ec765e3e6b6fe8fbb3ddf20f82d5295a37`；管理端 `moon/lunanexa-web@sha256:3071cd930f96519648206be5e5166c5feb9776a0542c58ed3da90dec599ce796`；企业端 `moon/lunanexa-web@sha256:0f112fbef35d5d781203978bb6d4c89aadb9a57aaf9c2dbba8e337afd283aba1`。三者均 `1/1 Ready`；公网 `:8443/mana/`、`:8443/user/`、`:8443/docs/` 及 `:5005/` 为 HTTP 200，线上管理端和企业端 JS 的 SHA-256 与构建产物一致。回滚可用上方“验证与发布”所列的 `r1` 摘要。
- 标准 443 诊断：管理节点本机 `127.0.0.1:443`、内网 `192.168.2.175:443` 和从管理节点回环访问公网 443 均返回 200；独立测试机强制直连公网 IP 且绑定 Wi-Fi 网卡时，443 的 TCP 建连超时，而同一网卡访问公网 8443 的 TLS 约 0.06 秒完成并返回 200。本机代理/TUN 会给域名返回 `198.18.0.140`，但绕过代理并指定真实公网 IP 后仍是上述结果。在管理节点的 `br-ngfw-in` 同步抓包，8443 请求源 `116.237.181.136` 的 SYN 到达并转发至入口 Pod；同一测试窗口内，443 连接超时且没有相应 SYN 到达该节点。因此故障在节点之前的公网 443 上游／路由器 WAN 转发或 ACL，不能在应用里伪称已修复。
- 现场仍无当前有效、已签署、已获甲方批准并独立核验到账的承诺函订单；两张历史订单均关闭。`.178/.179` 宿主可用内存约 9–11 GiB，不满足整机空闲预检；本次未停宿主级 GLM 任务、未伪造合同或付款，也未宣称物理 IaaS/PaaS/MaaS 端到端验收通过。
- 本轮构建结束后已删除管理节点 `/tmp/lunanexa-lease-test.YhOWwp` 与三个精确命名的 `/tmp/lunanexa-*-r2` 临时构建目录，以及本轮推送／拉取临时日志；没有删除私有仓库镜像、模型、用户工作文件或历史证据。
