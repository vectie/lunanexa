apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lunanexa-offline-dispatcher-state
  namespace: lunanexa
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 20Gi
