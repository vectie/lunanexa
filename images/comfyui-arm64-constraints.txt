# Highest mutually compatible ARM64 PyTorch trio available at build time.
# The general requirements file leaves these three unpinned. ARM64 torchaudio
# currently tops out at 2.11.0, so newer torch/torchvision cannot resolve.
torch==2.11.0+cpu
torchvision==0.26.0+cpu
torchaudio==2.11.0+cpu
